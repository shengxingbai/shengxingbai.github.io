# 2026-08-25

## 构建个人学术站点（GitHub Pages / Jekyll）
- 在 `C:\Users\Administrator\WorkBuddy\githubPO` 初始化了一个 Jekyll 静态站点，含三大模块：简历(resume.html)、博客(blog.html + _posts)、荣誉(honors.html)。
- 技术栈：Jekyll + GitHub Pages，自定义 `_layouts`(default/post)、`_includes`(header/footer)、`assets/css/style.css`(学术墨蓝风格、响应式)、`assets/js/main.js`(移动端导航 + 博客分类筛选)。
- 已 `git init` 并提交首个 commit；GitHub 连接器未连接，未推送远程（README 含完整部署步骤）。
- 新增 `_preview/preview.html`(已 gitignore) 作为设计预览，复用正式样式，分类筛选可用。
- 注意：本机无 Ruby/Jekyll，未做本地 build；Liquid 语法均为标准用法，依赖 GitHub Pages 云端构建。
