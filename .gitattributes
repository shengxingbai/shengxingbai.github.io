# 统一换行符为 LF，避免 Windows/PyCharm 默认 CRLF 与仓库 LF 冲突
# 导致 git 误判「无改动」而无法提交/同步的问题（core.autocrlf=true 时尤其明显）
* text=auto eol=lf

# 明确声明这些为文本，强制按 LF 规范化
*.md text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.yml text eol=lf
*.json text eol=lf

# 二进制资源不转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
